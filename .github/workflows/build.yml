name: 🏗️ Data Build

on:
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  build:
    if: >-
      contains(github.event.issue.labels.*.name, 'data build')
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: config
        run: |
          echo "running config"

      - name: data loading
        run: |
          echo "running data loading"

      - name: dataset build
        run: |
          echo "running build dataset"

      - name: qaqc export
        run: |
          echo "running qaqc export"
